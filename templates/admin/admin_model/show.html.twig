{% extends 'admin/base.html.twig' %}

{% block title %}Model{% endblock %}

{% block body %}
    <h1>Model</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ model.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_model_index') }}">back to list</a>

    <a href="{{ path('app_admin_model_edit', {'id': model.id}) }}">edit</a>

    {{ include('admin/admin_model/_delete_form.html.twig') }}
{% endblock %}
